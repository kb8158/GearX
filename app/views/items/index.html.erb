<h2 class="text-center"><%=link_to "<button>Add New Item</button>".html_safe, new_item_path %></h2>

<% @items.each do |item| %>
    <div>
      <p><%= link_to item.name, item_path(item)%> </p>
      <%= image_tag(item.image) %>
    </div>
  <% end %>


<%=link_to "<button>Add an Item </button>".html_safe, new_item_path %>
